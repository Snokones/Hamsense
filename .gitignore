# Secrets — never commit (WiFi creds, MACs)
secrets.h
satellite_secrets.h

# Generated / build artifacts
build/
*.bin
*.elf
*.hex

# Claude Code machine-local settings (per-user permission grants, not project config)
.claude/
